<div class="card p-4 rounded shadow-lg" style="margin:1rem 0 0 0;">
  <%= simple_form_for(@assistant) do |f| %>
    <div class="row justify-content-center align-items-center">
      <div class="col-12 col-md-6 col-lg-6">
        <%= f.input :firstname, placeholder: "Write your given name", label: false %>
      </div>
      <div class="col-12 col-md-6 col-lg-6">
        <%= f.input :lastname, placeholder: "Write your last name", label: false  %>
      </div>
    </div>
    <div class="row justify-content-center align-items-center">
      <div class="d-flex justify-content-start px-2 col-12 col-md-12 col-lg-12">
        <%= f.input :skills, as: :check_boxes, collection: %w[Organization Problem-Solving Flexibility Communication Creativity Responsibility Computer-proficiency Customer-service Research Computer-Software Marketing Content-creation Administrative Writing-editing Languages Design], include_blank: false, class: "text-primary" %>
      </div>
      <div class="col-12 col-md-12 col-lg-12">
        <%= f.input :availability, collection: %w[Any Part-time Full-time] %>
        <%= f.input :cv, as: :file, placeholder: "Must be PDF or DOC type" %>
        <%= f.input :photo, as: :file %>
      </div>
      <%= f.submit "Save changes", id:"Sign-up-btn", class: "profile-btn w-75 btn-sm" %>
    <div class="register">
      <% if !current_user.assistant.nil? %>
        <%= link_to "Back", assistant_path, class:"register-btn" %>
      <% else %>
        <%= link_to "Back", offers_path, class:"register-btn" %>
      <% end %>
    </div>
  <% end %>
</div>
